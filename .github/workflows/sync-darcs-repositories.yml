name: Sync Darcs Repositories

on:
  schedule:
    # Run every day at midnight UTC
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-darcs-repositories
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    container:
      image: debian:bookworm
    steps:
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y darcs git ca-certificates rsync
          darcs --version
          git --version
          rsync --version

      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Sync basilisk-source from Darcs
        run: ./scripts/sync-darcs-mirrors.sh source

      - name: Sync basilisk-wiki from Darcs
        run: ./scripts/sync-darcs-mirrors.sh wiki

      - name: Check for changes
        id: check_changes
        run: |
          if [ -n "$(git status --porcelain basilisk-source)" ] || [ -n "$(git status --porcelain basilisk-wiki)" ]; then
            echo "changes=true" >> "$GITHUB_OUTPUT"
            echo "Changes detected in repositories."
          else
            echo "changes=false" >> "$GITHUB_OUTPUT"
            echo "No changes detected in repositories."
          fi

      - name: Commit and push if changes exist
        if: steps.check_changes.outputs.changes == 'true'
        run: |
          if [ -n "$(git status --porcelain basilisk-source)" ]; then
            echo "Staging basilisk-source..."
            git add basilisk-source
            if git diff --cached --quiet -- basilisk-source; then
              echo "No committable basilisk-source changes."
            else
              git commit -m "Auto-update basilisk-source from Darcs repository"
            fi
          fi

          if [ -n "$(git status --porcelain basilisk-wiki)" ]; then
            echo "Staging basilisk-wiki..."
            git add basilisk-wiki
            if git diff --cached --quiet -- basilisk-wiki; then
              echo "No committable basilisk-wiki changes."
            else
              git commit -m "Auto-update basilisk-wiki from Darcs repository"
            fi
          fi

          git push
